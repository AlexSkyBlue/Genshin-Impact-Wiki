import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Angular Prueba',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
